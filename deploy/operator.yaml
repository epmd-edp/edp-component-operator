apiVersion: apps/v1
kind: Deployment
metadata:
  name: edp-component-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: edp-component-operator
  template:
    metadata:
      labels:
        name: edp-component-operator
    spec:
      serviceAccountName: edp-component-operator
      containers:
        - name: edp-component-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - edp-component-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "edp-component-operator"
